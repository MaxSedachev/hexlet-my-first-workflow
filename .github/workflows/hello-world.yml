name: Hello World Workflow

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  say_hello:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Say Hello
      run: make say-hello